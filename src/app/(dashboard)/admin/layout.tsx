import React from 'react';

export const metadata = {
  title: 'Admin Dashboard | Paperless Campus – URS Cainta',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

